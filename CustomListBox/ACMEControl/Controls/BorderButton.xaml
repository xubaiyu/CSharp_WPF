<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:ACMEControl.Controls">
    <Style TargetType="{x:Type local:BorderButton}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:BorderButton}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <Grid>
                            <Path Height="{TemplateBinding Height}" Width="{TemplateBinding Width}" Data="{TemplateBinding BorderData}" 
                                  Stroke="{TemplateBinding BorderStroke}" Fill="{TemplateBinding BorderFill}"/>
                            <Border Background="{TemplateBinding IconBrush}" OpacityMask="{TemplateBinding IconBrushMask}" Margin="{TemplateBinding IconMargin}"/>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>

